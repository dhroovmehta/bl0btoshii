# Mootoshi

Automated content production pipeline for **Blobtoshi** — an NES-style pixel art short-form video series featuring 6 animal characters on an island.

## Setup

1. Copy `.env.example` to `.env` and fill in credentials
2. Install dependencies: `pip install -r requirements.txt`
3. Run the Discord bot: `python -m src.bot.bot`

## Project Structure

See `Mootoshi_Technical_PRD_v2.md` for full documentation.
